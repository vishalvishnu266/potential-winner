{
  "name": "mobileapp",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor + Go/WASM mobile application",
  "scripts": {
    "build:wasm": "GOARCH=wasm GOOS=js go build -o dist/app.wasm .",
    "build:wasm:win": "powershell -Command \"$env:GOARCH='wasm'; $env:GOOS='js'; go build -o dist/app.wasm .\"",
    "copy:runtime": "node scripts/copy-runtime.js",
    "copy:static": "node scripts/copy-static.js",
    "build": "npm run build:wasm && npm run copy:runtime && npm run copy:static",
    "build:win": "npm run build:wasm:win && npm run copy:runtime && npm run copy:static",
    "sync": "npm run build && npx cap sync",
    "run:android": "npm run sync && npx cap open android",
    "run:ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
