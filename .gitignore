ota-demo/leptos-app/target/
ota-demo/leptos-app/dist/
# stylance-cli output — regenerated by the Trunk pre-build hook.
# The placeholder file itself IS committed (needed so Trunk can resolve
# the <link> in index.html on a fresh clone before stylance runs).
# Ignore local modifications to the generated content so noisy diffs
# don't pile up during development.
# ota-demo/leptos-app/assets/generated/stylance.css
ota-demo/axum-server/target/
ota-demo/axum-server/bundles/*.zip
ota-demo/axum-server/bundles/latest.json
ota-demo/capacitor-android/node_modules/
