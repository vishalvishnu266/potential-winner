{
  "name": "@app/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev":              "APP_ENV=mock APP_NAME=worker vite",
    "dev:real":         "APP_ENV=dev  APP_NAME=worker vite",
    "build":            "tsc --noEmit && APP_ENV=mock APP_NAME=worker vite build",
    "build:mock":       "tsc --noEmit && APP_ENV=mock APP_NAME=worker vite build",
    "build:dev":        "tsc --noEmit && APP_ENV=dev  APP_NAME=worker vite build",
    "build:prod":       "tsc --noEmit && APP_ENV=prod APP_NAME=worker vite build",
    "build:prod:indigo": "tsc --noEmit && APP_ENV=prod APP_NAME=worker APP_ACCENT=indigo  vite build",
    "build:prod:violet": "tsc --noEmit && APP_ENV=prod APP_NAME=worker APP_ACCENT=violet  vite build",
    "build:prod:sky":    "tsc --noEmit && APP_ENV=prod APP_NAME=worker APP_ACCENT=sky     vite build",
    "build:prod:emerald":"tsc --noEmit && APP_ENV=prod APP_NAME=worker APP_ACCENT=emerald vite build",
    "build:prod:amber":  "tsc --noEmit && APP_ENV=prod APP_NAME=worker APP_ACCENT=amber   vite build",
    "build:prod:rose":   "tsc --noEmit && APP_ENV=prod APP_NAME=worker APP_ACCENT=rose    vite build",
    "build:prod:slate":  "tsc --noEmit && APP_ENV=prod APP_NAME=worker APP_ACCENT=slate   vite build",
    "preview":          "vite preview",
    "typecheck":        "tsc --noEmit",
    "android":          "npm run build && npx cap sync android && npx cap run android",
    "android:real":     "npm run build:dev && npx cap sync android && npx cap run android",
    "ios":              "npm run build && npx cap sync ios && npx cap open ios",
    "ios:real":         "npm run build:dev && npx cap sync ios && npx cap open ios",
    "bundle:ota":       "node ../../scripts/build-bundle.mjs --app=worker",
    "bundle:ota:prod":  "APP_ENV=prod node ../../scripts/build-bundle.mjs --app=worker",
    "cap:add:android":  "npx cap add android",
    "cap:add:ios":      "npx cap add ios"
  },
  "dependencies": {
    "@pkg/api-contracts": "*",
    "@pkg/i18n": "*",
    "@pkg/native": "*",
    "@pkg/ota": "*",
    "@pkg/theme": "*",
    "@pkg/ui": "*",
    "@pkg/vite-config": "*",
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/preferences": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.0",
    "@capgo/capacitor-updater": "^8.51.3",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "zustand": "^4.5.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^6.0.5",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.6.3",
    "vite": "^8.2.0"
  }
}
