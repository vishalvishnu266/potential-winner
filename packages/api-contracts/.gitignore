# Everything under src/generated/ is codegen output written by
# `@hey-api/openapi-ts` (see `openapi-ts.config.ts`).  We do NOT commit
# it — a fresh clone regenerates it via `npm run api:sync`, which the
# root `postinstall` hook runs automatically after `npm install`.
src/generated/
