# Everything under src/generated/ is codegen output — but we DO commit the
# placeholder index.ts so a fresh clone works without running codegen.
# Only ignore the *.gen.ts files that `@hey-api/openapi-ts` emits.
src/generated/*.gen.ts
