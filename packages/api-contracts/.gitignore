# TypeScript types generated by `openapi-typescript` from `openapi.json`.
# Not committed — a fresh clone regenerates it via `npm run api:codegen`,
# which the root `postinstall` hook runs automatically after `npm install`.
src/generated/
